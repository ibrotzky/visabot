# Put any properties here if not set in the env variables.
# Entries will NOT override system environment variables.

# Example:
#GP_FOO_SETTING=BAR

# You can put lines like the following in a file 'local-test.properties' for local tests:
#GP_URL=http://localhost:9080/translate/rest
#GP_USER_ID=<userid>
#GP_PASSWORD=<userpass>
#GP_INSTANCE_ID=<instanceId>
#GP_VERBOSE=true

# *for internal use only*
#GP_ADMIN_ID=<adminid>
#GP_ADMIN_PASSWORD=<adminpass>
